#include "agents/ActorInfo.h"
#include "entities/Actor.h"

ActorInfo::ActorInfo(Actor * actor) : name(actor->getName()) {
//		setActionType(actor->getActionType());
		setDirection(actor->getDirection());
		setPosition(actor->getPosition());
		setTeam(actor->getTeam());
		setWeaponType(actor->getCurrentWeapon());
		setHealth(actor->getHealth());
		setArmour(actor->getArmour());
	}